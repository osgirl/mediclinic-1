<?xml version="1.0" encoding="UTF-8"?>
<web-app version="2.4" xmlns="http://java.sun.com/xml/ns/j2ee"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://java.sun.com/xml/ns/j2ee 
	http://java.sun.com/xml/ns/j2ee/web-app_2_4.xsd">
	<!-- 默认bean配置文件 
		它将成为spring的root同时也是最先促使话的
		一般不在里面做任何配置 
		如有需要一定要最先加载的bean才在这里配置 -->
	<context-param>
		<param-name>contextConfigLocation</param-name>
		<param-value>/WEB-INF/spring.xml</param-value>
	</context-param>
	<context-param>
		<param-name>encoding</param-name>
		<param-value>UTF-8</param-value>
	</context-param>
	<listener>
		<listener-class>
			org.springframework.web.context.ContextLoaderListener
		</listener-class>
	</listener>
	<!-- spring security监听器 -->
	<listener>
		<listener-class>
			org.springframework.security.ui.session.HttpSessionEventPublisher
		</listener-class>
	</listener>
	<!-- 将浏览器发过来的数据强行转成UTF-8
	-->
	<filter>
		<filter-name>encodeFilter</filter-name>
		<filter-class>
			cn.org.seventh.web.common.EncoderFilter
		</filter-class>
	</filter>
	<!-- spring security过滤器 有名的filter2bean 
		默认的只会从context中去
		但是改变了bean加载地点
		默认的context这个root容器中没有beans
	-->
	<filter>
		<filter-name>springSecurityFilterChain</filter-name>
		<filter-class>
			cn.org.seventh.web.common.CustomDelegatingFilterProxy
		</filter-class>
		<init-param>
			<param-name>dispatcherServletName</param-name>
			<param-value>DispatcherServlet</param-value>
		</init-param>
	</filter>
	<filter-mapping>
		<filter-name>springSecurityFilterChain</filter-name>
		<url-pattern>/action/*</url-pattern>
	</filter-mapping>
	<!-- 标准spring dispatcher servlet配置 -->
	<servlet>
		<servlet-name>DispatcherServlet</servlet-name>
		<servlet-class>
			org.springframework.web.servlet.DispatcherServlet
		</servlet-class>
		<init-param>
			<param-name>namespace</param-name>
			<param-value>spring</param-value>
		</init-param>
		<init-param>
			<param-name>contextConfigLocation</param-name>
			<param-value>classpath:/spring/spring-*.xml</param-value>
		</init-param>
		<load-on-startup>1</load-on-startup>
	</servlet>
	<servlet-mapping>
		<servlet-name>DispatcherServlet</servlet-name>
		<url-pattern>/action/*</url-pattern>
	</servlet-mapping>
	<!-- FCK EDITOR FILE UP LOAD -->
	<servlet>
		<servlet-name>FileBrowser</servlet-name>
		<servlet-class>
			cn.org.seventh.web.common.FCKConnectorServlet
		</servlet-class>
		<init-param>
			<param-name>baseDir</param-name>
			<param-value>/upload/</param-value>
		</init-param>
		<init-param>
			<param-name>debug</param-name>
			<param-value>true</param-value>
		</init-param>
		<load-on-startup>1</load-on-startup>
	</servlet>
	<servlet-mapping>
		<servlet-name>FileBrowser</servlet-name>
		<url-pattern>
			/commons/editor/filemanager/browser/default/connectors/jsp/connector.jsp
		</url-pattern>
	</servlet-mapping>
	<servlet>
		<servlet-name>QuickUpload</servlet-name>
		<servlet-class>
			cn.org.seventh.web.common.FCKSimpleUploaderServlet
		</servlet-class>
		<init-param>
			<param-name>baseDir</param-name>
			<param-value>/upload/</param-value>
		</init-param>
		<init-param>
			<param-name>debug</param-name>
			<param-value>true</param-value>
		</init-param>
		<init-param>
			<param-name>enabled</param-name>
			<param-value>true</param-value>
		</init-param>
		<init-param>
			<param-name>AllowedExtensionsFile</param-name>
			<param-value></param-value>
		</init-param>
		<init-param>
			<param-name>DeniedExtensionsFile</param-name>
			<param-value>
				php|php3|php5|phtml|asp|aspx|ascx|jsp|cfm|cfc|pl|bat|exe|dll|reg|cgi
			</param-value>
		</init-param>
		<init-param>
			<param-name>AllowedExtensionsImage</param-name>
			<param-value>jpg|gif|jpeg|png|bmp</param-value>
		</init-param>
		<init-param>
			<param-name>DeniedExtensionsImage</param-name>
			<param-value></param-value>
		</init-param>
		<init-param>
			<param-name>AllowedExtensionsFlash</param-name>
			<param-value>swf|fla</param-value>
		</init-param>
		<init-param>
			<param-name>DeniedExtensionsFlash</param-name>
			<param-value></param-value>
		</init-param>
		<load-on-startup>1</load-on-startup>
	</servlet>
	<servlet-mapping>
		<servlet-name>QuickUpload</servlet-name>
		<url-pattern>
			/commons/editor/filemanager/upload/jsp/upload.jsp
		</url-pattern>
	</servlet-mapping>
	<!-- FCK EDITOR FILE UP LOAD -->
	<!--  如果有自己写的TAG请在这里配置路径
		<jsp-config>
		<taglib> 
		<taglib-uri>http://www.seventh.org.cn/tags</taglib-uri>
		<taglib-location>/WEB-INF/classes/taglib.tld</taglib-location>
		</taglib>
		</jsp-config>
	-->
	<!-- 标准首页默认文件列表 -->
	<welcome-file-list>
		<welcome-file>index.jsp</welcome-file>
	</welcome-file-list>
</web-app>
